@model List<FieldWithValue>

@{
    ViewData["Title"] = "Formularz";
}

<h3>Wypełnij fromularz</h3>

<style>

    .cities {
        background-color: #66BB6A;
        color: white;
        margin: 20px;
        padding: 20px;
    }
</style>
<div class="cities">
    <div class="row">
        <div class="col-md-10">
            <form asp-action="Formularz">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <table class="table table-striped">

                    @for (var i = 0; i < Model.Count; i++)
                    {
                    <tr>
                        <input asp-for="@Model[i].Field.Id" hidden />
                        <input asp-for="@Model[i].Field.Name" hidden />
                        <input asp-for="@Model[i].Field.Type" hidden />
                        @if (Model[i].Field.Type == "text")
                        {
                            <div>
                                <label asp-for="@Model[i].Field.Name" class="control-label">@Model[i].Field.Name</label>
                            </div>

                            <div>
                                <input class="form-control" type="@Model[i].Field.Type" asp-for="@Model[i].TextValue" />
                            </div>
                            <div>
                                <span asp-validation-for="@Model[i].Field.Type" class="text-danger"></span>
                            </div>



                        }
                        else if (Model[i].Field.Type == "checkbox")
                        {


                            <div>
                                <label asp-for="@Model[i].Field.Name" class="control-label">@Model[i].Field.Name</label>
                            </div>
                            <div>
                                <input class="form-control checkmark" type="@Model[i].Field.Type" asp-for="@Model[i].BoolValue" />
                            </div>
                            <div>
                                <span asp-validation-for="@Model[i].Field.Name" class="text-danger"></span>
                            </div>
                        }

                        else if (Model[i].Field.Type == "number")
                        {


                            <div>
                                <label asp-for="@Model[i].Field.Name" class="control-label">@Model[i].Field.Name</label>
                            </div>
                            <div>
                                <input class="form-control" numInput="true" type="text" asp-for="@Model[i].TextValue" />
                            </div>
                            <div>
                                <span asp-validation-for="@Model[i].Field.Name" class="text-danger"></span>
                            </div>


                        }
                    </tr>
                    }
                </table>
                <div class="form-group">
                </div>
                <div>
                    <input style=" background-color:white;
            color:#66BB6A;
            font-size:20px;
            padding: 15px 35px;
            border-radius: 8px;
            position:center;" type="submit" asp-route-formId="@ViewBag.formid" value="Wyślij formularz" class="btn btn-primary" />
                </div>
               
            </form>
          
        </div>
    </div>
</div>






<script type="text/javascript">
    
  var input_button = $(".addinput");
  var checkbox_button = $(".addcheckbox");
  var i=@ViewBag.modelcount;
  var questions = $(".questions");
    
  $(".addinput").click(function(e){
    var td = $("<td class=line /> ");
    var name = $("<input />").attr("type","text").attr("id","fields["+i+"].Name").attr("class","name")
      var text = $("<input />").attr("type", "text").attr("id", "fields[" + i + "].Answer").attr("class", "answer")
      var span = $("<span />").attr("asp-validation-for",)
    td.append(name);
    td.append(text);
    var buttonrm = $($("<input />").attr("type", "button").attr("value", "remove").attr("class","remove_field"))
    var buttonadd = $($("<input />").attr("type", "button").attr("value", "save").attr("class","save_field"))
    e.preventDefault();
    div.append(buttonrm);
    div.append(buttonadd);
    $(".questions").append(div);
    i++;
  });

  $(".addcheckbox").click(function(e){
    e.preventDefault();
    var checkbox = $("<input />").attr("type","text").attr("id","fields["+i+"]")
    $(".questions").append('<div><input type="text" class="name"><input type="checkbox" name="checkbox"+i+"/><a href="#" class="remove_field">Remove</a><a href="#" class="save_field">Save</a></div>')
    i++;
  });


  $(questions).on("click",".remove_field", function(e){ //user click on remove text
		e.preventDefault(); $(this).parent('div').remove(); i--;
	});


$(questions).on("click",".save_field", function(e){ //user click on remove text
		e.preventDefault() ;
  
  if(!($(this).children().first().val()===""))
    {      
      console.log("test");
    $(".name").first().replaceWith("<label id=fields["+i+"].Name class=name>"+$(".name").val()+"</label>");
    $(this).remove();
    i--;
    }
});

    $(function(){
       $("input[numInput='true']").keypress(function (e) {
           console.log(e.which)
           if (e.which == 49 || e.which == 50 || e.which == 51 || e.which == 52 || e.which == 53 || e.which == 54 || e.which == 55 || e.which == 56 || e.which == 57 || e.which == 48 || e.which == 44 || e.which==45) {
    } else {
      return false;
    }
  });
   });


</script>