@model IEnumerable<FormGenerator.Models.PatientFormsHelper>

@{
    ViewData["Title"] = "Index";
}
@*<style>
  body {
      margin-left:50px;
  overflow-x: hidden;
}

#sidebar-wrapper {
  min-height: 100vh;
  margin-left: -15rem;
  -webkit-transition: margin .25s ease-out;
  -moz-transition: margin .25s ease-out;
  -o-transition: margin .25s ease-out;
  transition: margin .25s ease-out;
}

#sidebar-wrapper .sidebar-heading {
  padding: 0.875rem 1.25rem;
  font-size: 1.2rem;
}

#sidebar-wrapper .list-group {
  width: 15rem;
}

#page-content-wrapper {
  min-width: 100vw;
}

#wrapper.toggled #sidebar-wrapper {
  margin-left: 0;
}


  #page-content-wrapper {
    min-width: 0;
    width: 100%;
  }

  #wrapper.toggled #sidebar-wrapper {
    margin-left: -15rem;
  }
}
</style>


@if (TempData["lista"] != null)
{
<body>

    <div class="d-flex" id="wrapper">

    
        <div class="bg-light border-right" id="sidebar-wrapper">
            <div class="sidebar-heading">Formularze </div>
            <div class="list-group list-group-flush">
                <div class="table">
                    <table class="table">
                        <thead>

                        </thead>
                        <tbody>
                            @foreach (var item in (IEnumerable<PatientFormsHelper>)TempData["lista"])
                            {
                                <tr>

                                    <td class="btn-primary">
                                        @Html.DisplayFor(modelItem => item.nazwa_formularza)
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </body>
}*@





<div class="root">
       
</div>
